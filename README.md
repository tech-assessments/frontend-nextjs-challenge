# 🚀 Next.js Frontend Challenge

## 🧱 توضیح پروژه  
در این تسک باید یک **Dashboard مدیریتی کاربران** بسازید که شامل صفحات زیر باشد:

---

### 1. صفحه‌ی اصلی `/`  
- نمایش پیام خوش‌آمدگویی.  
- دکمه‌ای برای رفتن به صفحه‌ی کاربران.  
- *(اختیاری)* نمایش چند آمار ساده مانند تعداد کاربران یا شرکت‌ها (می‌توانید با fetch از API محاسبه کنید).

---

### 2. صفحه‌ی لیست کاربران `/users`  
- داده‌ها را از API زیر دریافت کنید:  
  👉 [`https://jsonplaceholder.typicode.com/users`](https://jsonplaceholder.typicode.com/users)
- کاربران را در قالب جدول یا کارت نمایش دهید.  
- ستون‌های پیشنهادی:
  - `Name`
  - `Email`
  - `Company`
  - `City`
- امکانات مورد انتظار:
  - 🔍 جست‌وجوی کاربران بر اساس نام یا ایمیل  
  - 🏷️ فیلتر بر اساس شرکت یا شهر  
  - 🌀 نمایش حالت Loading و Error در هنگام دریافت داده  
  - 📱 طراحی واکنش‌گرا (Responsive)

با کلیک روی هر کاربر، باید به صفحه‌ی جزئیات هدایت شوید.

---

### 3. صفحه‌ی جزئیات کاربر `/users/[id]`  
- داده‌ی کاربر را با `id` از همان API دریافت کنید.  
- فیلدهای پیشنهادی برای نمایش:
  - `name`, `username`, `email`, `phone`, `address.city`, `company.name`
- نمایش دکمه‌ی "بازگشت" برای برگشت به `/users`.  
- *(اختیاری)* دکمه‌ی "Edit" برای ویرایش اطلاعات کاربر در UI (بدون نیاز به ذخیره در API).

---

### 4. صفحه‌ی ایجاد یا ویرایش کاربر `/users/new` *(اختیاری – امتیاز مثبت 🌟)*  
- یک فرم برای ایجاد یا ویرایش کاربر طراحی کنید.  
- نیازی به ذخیره واقعی در API نیست، اما فرم باید دارای ولیدیشن و مدیریت state باشد.  
- می‌توانید از `React Hook Form` یا `Formik` استفاده کنید.

---

## 🧭 نحوه انجام تسک  

1. ریپازیتوری را clone کنید:  
   ```bash
   git clone <repo-url>
2. یک branch جدید با نام خودتان بسازید:
   ```bash
   git checkout -b feature/<your-name>
  
3. پروژه را توسعه دهید.  
4. تغییرات را commit و push کنید.  
5. در نهایت، یک Pull Request ارسال کنید تا بررسی انجام شود.   
